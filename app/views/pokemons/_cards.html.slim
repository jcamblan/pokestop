.pokemon__cards
  .col-12.col-lg-6
    = render 'pokemons/cards/stats'
    - unless @pokemon.eggs.empty?
      = render 'pokemons/cards/eggs'
  .col-12.col-lg-5
    - if @evolutions
      = render 'pokemons/cards/evolutions'
    - if @pokemon.attacks.present?
      = render 'pokemons/cards/attacks'